<% if review.like_user(current_user.id) %>
    <%= button_to review_like_path(review_id: review.id), method: :delete, id: "like-button", remote: true do %>
     お気に入り済み
    <% end %>
  <% else %>
    <%= button_to review_likes_path(review_id: review.id), method: :post, id: "like-button", remote: true do %>
      お気に入り
    <% end %>
  <% end %>